{
  "name": "webgpu-engine",
  "private": false,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --config vite.lib.config.ts --watch",
    "build": "vite build --config vite.lib.config.ts && tsc -p tsconfig.lib.json && node scripts/remove-empty-dts.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "fbx-parser": "^2.1.3"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "@webgpu/types": "^0.1.69",
    "typescript": "~5.9.3",
    "vite": "^8.0.1",
    "vitest": "^4.1.2"
  }
}
