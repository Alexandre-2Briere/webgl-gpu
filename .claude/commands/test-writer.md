---
name: test-writer
description: Write Vitest unit tests for changed or specified source files, covering all code paths with mocks, edge cases, and data variants.
invocation: /test-writer
---

Spawn the `test-writer` sub-agent to write or update Vitest unit tests for all changed TypeScript files inside `src/webgpu/`.
